/*
 * Add API endpoints only after the backend contract and final app screens are known.
 * Keeping this empty prevents assumed business flows from leaking into the handover.
 */
export const ApiEndpoint = {} as const
